# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Data (CRSP/Compustat pulls, caches) — keep out of git
data/
*.parquet
*.csv

# Secrets — never commit API keys
.env
.tiingo_token
*.token
