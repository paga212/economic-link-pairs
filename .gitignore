# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Data (CRSP/Compustat pulls, caches) — keep out of git
data/
*.parquet
*.csv

# Secrets — never commit API keys
.env
.tiingo_token
.anthropic_key
.anthropic_token
*.token
*.key

# Run logs + generated dashboard/digest
paper_run.log
*.log
dashboard.html
digest.json
site/
